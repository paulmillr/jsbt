/** Adds two numbers, but this documentation line is intentionally long enough to exceed one hundred characters and should be reworded.
 * @param a - First addend.
 * @param b - Second addend.
 * @returns Sum of both addends.
 * @example
 * Add two numbers.
 * ```ts
 * import { add } from '@jsbt-test/check-src/alpha.js';
 * add(20, 22);
 * ```
 */
export const add = (a: number, b: number): number => a + b;
