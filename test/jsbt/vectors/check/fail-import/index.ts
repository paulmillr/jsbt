/** Fast root export.
 * @returns Fast value.
 * @example
 * Use the root export.
 * ```ts
 * import { fast } from '@jsbt-test/check-import-fail';
 * console.log(fast);
 * ```
 */
export const fast = 1;
