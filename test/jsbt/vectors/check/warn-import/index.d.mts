/** Fast root export.
 * @returns Fast value.
 * @example
 * Use the root export.
 * ```ts
 * import { fast } from '@jsbt-test/check-import';
 * console.log(fast);
 * ```
 */
export declare const fast: 1;
